/** Cropped photo of the top tag only (1024×266). */
export const MOCKUP_PHOTO = {
  src: "/keytag-mockup-top.png",
  width: 1024,
  height: 266,
};

/** Full recess on photo (pic + QR). Simple rect + trapezoid clip — no quad warp. */
export const MOCKUP_ART_PIXELS = {
  x: 334,
  y: 80,
  w: 472,
  h: 188,
};
