/** Cropped photo of the top tag only (1024×266). */
export const MOCKUP_PHOTO = {
  src: "/keytag-mockup-top.png",
  width: 1024,
  height: 266,
};

/** Full recess (pic + QR) on the product photo. */
export const MOCKUP_ART_PIXELS = {
  x: 325,
  y: 79,
  w: 488,
  h: 192,
};

/**
 * Counter-clockwise tilt around bottom-right corner — drops the left edge down.
 * (Positive canvas rotation = clockwise = wrong; negative = left side down.)
 */
export const MOCKUP_ROTATE_RAD = -0.014;
